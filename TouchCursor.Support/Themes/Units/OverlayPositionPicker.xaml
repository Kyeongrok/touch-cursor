<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:units="clr-namespace:TouchCursor.Support.UI.Units">

    <Style x:Key="PositionToggleButton" TargetType="ToggleButton">
        <Setter Property="Width" Value="18"/>
        <Setter Property="Height" Value="18"/>
        <Setter Property="Margin" Value="1"/>
        <Setter Property="Background" Value="#E0E0E0"/>
        <Setter Property="BorderBrush" Value="#BDBDBD"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="#2196F3"/>
                            <Setter Property="BorderBrush" Value="#1976D2"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#BDBDBD"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="#1E88E5"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type units:OverlayPositionPicker}">
        <Setter Property="Width" Value="66"/>
        <Setter Property="Height" Value="66"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type units:OverlayPositionPicker}">
                    <Border Background="#F5F5F5" BorderBrush="#BDBDBD" BorderThickness="1" CornerRadius="4" Padding="2">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Row 0 -->
                            <ToggleButton x:Name="PART_TopLeft" Grid.Row="0" Grid.Column="0"
                                          Style="{StaticResource PositionToggleButton}"/>
                            <ToggleButton x:Name="PART_TopCenter" Grid.Row="0" Grid.Column="1"
                                          Style="{StaticResource PositionToggleButton}"/>
                            <ToggleButton x:Name="PART_TopRight" Grid.Row="0" Grid.Column="2"
                                          Style="{StaticResource PositionToggleButton}"/>

                            <!-- Row 1 -->
                            <ToggleButton x:Name="PART_MiddleLeft" Grid.Row="1" Grid.Column="0"
                                          Style="{StaticResource PositionToggleButton}"/>
                            <ToggleButton x:Name="PART_MiddleCenter" Grid.Row="1" Grid.Column="1"
                                          Style="{StaticResource PositionToggleButton}"/>
                            <ToggleButton x:Name="PART_MiddleRight" Grid.Row="1" Grid.Column="2"
                                          Style="{StaticResource PositionToggleButton}"/>

                            <!-- Row 2 -->
                            <ToggleButton x:Name="PART_BottomLeft" Grid.Row="2" Grid.Column="0"
                                          Style="{StaticResource PositionToggleButton}"/>
                            <ToggleButton x:Name="PART_BottomCenter" Grid.Row="2" Grid.Column="1"
                                          Style="{StaticResource PositionToggleButton}"/>
                            <ToggleButton x:Name="PART_BottomRight" Grid.Row="2" Grid.Column="2"
                                          Style="{StaticResource PositionToggleButton}"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
