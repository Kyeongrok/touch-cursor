<Window x:Class="touch_cursor.KeyMappingEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit Key Mapping" Height="650" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Source Key Section -->
        <GroupBox Grid.Row="0" Header="Source Key" Margin="0,0,0,20" Padding="5">
            <StackPanel Margin="10">
                <TextBlock TextWrapping="Wrap" Margin="0,0,0,10">
                    Press the key you want to map (while holding the activation key):
                </TextBlock>
                <Border BorderBrush="Gray" BorderThickness="1" Padding="15,10" Background="White" Margin="0,0,0,10">
                    <TextBlock x:Name="SourceKeyTextBlock" Text="(Not set)" FontWeight="Bold" FontSize="14"/>
                </Border>
                <Button x:Name="CaptureSourceKeyButton" Content="Capture Key..." Width="120" Height="30"
                        Margin="0,5,0,0" HorizontalAlignment="Left"
                        Click="CaptureSourceKeyButton_Click"/>
            </StackPanel>
        </GroupBox>

        <!-- Target Key Section -->
        <GroupBox Grid.Row="1" Header="Target Key" Margin="0,0,0,20" Padding="5">
            <StackPanel Margin="10">
                <TextBlock TextWrapping="Wrap" Margin="0,0,0,10">
                    Press the key you want it to be mapped to:
                </TextBlock>
                <Border BorderBrush="Gray" BorderThickness="1" Padding="15,10" Background="White" Margin="0,0,0,10">
                    <TextBlock x:Name="TargetKeyTextBlock" Text="(Not set)" FontWeight="Bold" FontSize="14"/>
                </Border>
                <Button x:Name="CaptureTargetKeyButton" Content="Capture Key..." Width="120" Height="30"
                        Margin="0,5,0,0" HorizontalAlignment="Left"
                        Click="CaptureTargetKeyButton_Click"/>
            </StackPanel>
        </GroupBox>

        <!-- Description Section -->
        <GroupBox Grid.Row="2" Header="Description (Optional)" Margin="0,0,0,20" Padding="5">
            <StackPanel Margin="10">
                <TextBox x:Name="DescriptionTextBox" Height="70"
                         TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" Padding="5"/>
            </StackPanel>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="80" Height="30" Margin="0,0,10,0"
                    IsDefault="True" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Height="30"
                    IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
