<Window x:Class="touch_cursor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:touch_cursor"
        mc:Ignorable="d"
        Title="TouchCursor" Height="400" Width="500"
        WindowStartupLocation="Manual"
        ResizeMode="CanMinimize"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        StateChanged="Window_StateChanged">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleTextBlock" Grid.Row="0" FontSize="24" FontWeight="Bold" Margin="0,0,0,20">
            TouchCursor
        </TextBlock>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <CheckBox x:Name="EnabledCheckBox" Content="Enable TouchCursor"
                      IsChecked="True" FontSize="14" Margin="0,5"
                      Checked="EnabledCheckBox_Changed" Unchecked="EnabledCheckBox_Changed"/>
            <CheckBox x:Name="TrainingModeCheckBox" Content="Training Mode (beep for mistakes)"
                      FontSize="14" Margin="0,5"
                      Checked="TrainingModeCheckBox_Changed" Unchecked="TrainingModeCheckBox_Changed"/>
            <CheckBox x:Name="RunAtStartupCheckBox" Content="Run at Windows Startup"
                      FontSize="14" Margin="0,5"
                      Checked="RunAtStartupCheckBox_Changed" Unchecked="RunAtStartupCheckBox_Changed"/>
        </StackPanel>

        <GroupBox x:Name="QuickInfoGroupBox" Grid.Row="2" Header="Quick Info" Margin="0,0,0,20">
            <StackPanel Margin="10">
                <TextBlock x:Name="DefaultKeyMappingsTextBlock" TextWrapping="Wrap" Margin="0,5">
                    <Bold>Default Key Mappings (Space +):</Bold>
                </TextBlock>
                <TextBlock x:Name="ArrowKeysTextBlock" TextWrapping="Wrap" Margin="10,5">
                    • I/J/K/L = Arrow Keys (Up/Left/Down/Right)
                </TextBlock>
                <TextBlock x:Name="HomeEndTextBlock" TextWrapping="Wrap" Margin="10,5">
                    • U/O = Home/End
                </TextBlock>
                <TextBlock x:Name="PageUpDownTextBlock" TextWrapping="Wrap" Margin="10,5">
                    • H/P = Page Up/Down
                </TextBlock>
                <TextBlock x:Name="BackspaceDeleteTextBlock" TextWrapping="Wrap" Margin="10,5">
                    • M/, = Backspace/Delete
                </TextBlock>
                <TextBlock x:Name="WordNavigationTextBlock" TextWrapping="Wrap" Margin="10,5">
                    • N/. = Ctrl+Left/Right (word navigation)
                </TextBlock>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SettingsButton" Content="Settings" Width="100" Height="30"
                    Margin="0,0,10,0" Click="SettingsButton_Click"/>
            <Button x:Name="MinimizeButton" Content="Minimize to Tray" Width="120" Height="30"
                    Click="MinimizeButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
