<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:views="clr-namespace:TouchCursor.Main.UI.Views"
                    xmlns:loc="clr-namespace:TouchCursor.Main.Localization"
                    xmlns:units="clr-namespace:TouchCursor.Support.UI.Units;assembly=TouchCursor.Support">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/TouchCursor.Support;component/Themes/Units/ModernComboBox.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type views:GeneralSettingsView}">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type views:GeneralSettingsView}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="5">
                                <units:ToggleSwitch Content="{loc:Localize SettingsWindow.EnableTouchCursor, Default='Enable TouchCursor'}"
                                                    IsChecked="{Binding IsEnabled}" Margin="0,4"/>
                                <units:ToggleSwitch Content="{loc:Localize SettingsWindow.RunAtStartup, Default='Run at Windows startup'}"
                                                    IsChecked="{Binding RunAtStartup}" Margin="0,4"/>
                                <units:ToggleSwitch Content="{loc:Localize SettingsWindow.ShowInTray, Default='Show in system tray'}"
                                                    IsChecked="{Binding ShowInTray}" Margin="0,4"/>

                                <Separator Margin="0,8"/>

                                <!-- Language & Overlay Position -->
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="언어 / Language:" Margin="0,0,0,4" FontSize="12"/>
                                        <ComboBox ItemsSource="{Binding AvailableLanguages}"
                                                  SelectedValue="{Binding SelectedLanguage}"
                                                  SelectedValuePath="Code"
                                                  DisplayMemberPath="NativeName"
                                                  Width="150"
                                                  Style="{StaticResource ModernComboBoxStyle}"
                                                  ItemContainerStyle="{StaticResource ModernComboBoxItemStyle}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="오버레이 위치:" Margin="0,0,0,4" FontSize="12"/>
                                        <units:OverlayPositionPicker SelectedPosition="{Binding OverlayPosition}"
                                                                     Width="66" Height="66"/>
                                    </StackPanel>
                                </Grid>

                                <Separator Margin="0,8"/>

                                <!-- Hold Delay -->
                                <TextBlock Text="{loc:Localize SettingsWindow.HoldDelay, Default='Activation Key Hold Delay:'}" Margin="0,4,0,2" FontSize="12"/>
                                <Grid Margin="0,2,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Slider Grid.Column="0" Minimum="0" Maximum="300"
                                            TickFrequency="10" IsSnapToTickEnabled="True"
                                            Value="{Binding HoldDelayMs}" Margin="0,0,10,0"/>
                                    <TextBlock Grid.Column="1" Width="45" VerticalAlignment="Center"
                                               Text="{Binding HoldDelayMs, StringFormat='{}{0} ms'}" FontSize="12"/>
                                </Grid>

                                <!-- Rollover -->
                                <units:ToggleSwitch Content="{loc:Localize SettingsWindow.RolloverEnabled, Default='Enable Rollover'}"
                                                    IsChecked="{Binding RolloverEnabled}" Margin="0,4"/>
                                <TextBlock Text="{loc:Localize SettingsWindow.RolloverEnabledDescription, Default='When enabled, keys pressed before activation (Hold Delay) are treated as normal typing with the activation key.'}"
                                           TextWrapping="Wrap" Margin="42,0,0,4" FontSize="11" Foreground="Gray"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
