<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:units="clr-namespace:TouchCursor.Main.UI.Units"
                    xmlns:converters="clr-namespace:TouchCursor.Main.Converters"
                    xmlns:supportUnits="clr-namespace:TouchCursor.Support.UI.Units;assembly=TouchCursor.Support">

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>

    <Style TargetType="{x:Type units:TitleBar}">
        <Setter Property="Height" Value="32"/>
        <Setter Property="Background" Value="#F0F0F0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type units:TitleBar}">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Icon -->
                        <Image Grid.Column="0"
                               Source="{TemplateBinding Icon}"
                               Width="16" Height="16" Margin="8,0,0,0"
                               VerticalAlignment="Center"/>

                        <!-- Title or Tab Content -->
                        <Grid Grid.Column="1" Margin="8,0,0,0">
                            <TextBlock Text="{TemplateBinding Title}"
                                       VerticalAlignment="Center"
                                       FontSize="12" Foreground="#333"
                                       Visibility="{Binding TabContent, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}"/>
                            <ContentPresenter Content="{TemplateBinding TabContent}"
                                              VerticalAlignment="Center"
                                              WindowChrome.IsHitTestVisibleInChrome="True"/>
                        </Grid>

                        <!-- Window Buttons -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button x:Name="PART_HelpButton"
                                    Width="46" Height="32"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Content="?"
                                    FontSize="16" FontWeight="Bold"
                                    Foreground="#666"
                                    WindowChrome.IsHitTestVisibleInChrome="True"
                                    ToolTip="About"/>
                            <supportUnits:MinimizeButton x:Name="PART_MinimizeButton"
                                Visibility="{Binding ShowMinimizeButton, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <supportUnits:MaximizeButton x:Name="PART_MaximizeButton"
                                Visibility="{Binding ShowMaximizeButton, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <supportUnits:CloseButton x:Name="PART_CloseButton"/>
                        </StackPanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
