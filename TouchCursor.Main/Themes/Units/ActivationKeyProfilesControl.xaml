<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:units="clr-namespace:TouchCursor.Main.UI.Units"
                    xmlns:loc="clr-namespace:TouchCursor.Main.Localization">

    <!-- ActivationKeyProfilesControl Default Style -->
    <Style TargetType="{x:Type units:ActivationKeyProfilesControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type units:ActivationKeyProfilesControl}">
                    <GroupBox Header="{loc:Localize SettingsWindow.ActivationKeyProfiles, Default='Activation Key Profiles'}"
                              Margin="0,0,0,15">
                        <StackPanel Margin="10">
                            <TextBlock Text="{loc:Localize SettingsWindow.ActivationKeyProfilesDescription, Default='Manage multiple activation keys, each with their own key mappings:'}"
                                       TextWrapping="Wrap" Margin="0,0,0,10"/>
                            <ListBox x:Name="PART_ActivationKeyProfilesListBox"
                                     Height="60" Margin="0,0,0,10"
                                     ItemsSource="{TemplateBinding ActivationKeyProfiles}"
                                     SelectedItem="{Binding SelectedActivationKeyProfile, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding KeyName}" Width="150" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding MappingCount, StringFormat='({0} mappings)'}" Foreground="Gray"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="{loc:Localize SettingsWindow.AddProfile, Default='Add Profile...'}"
                                        Width="120" Margin="0,0,5,0"
                                        Command="{TemplateBinding AddActivationKeyProfileCommand}"/>
                                <Button Content="{loc:Localize SettingsWindow.RemoveProfile, Default='Remove Profile'}"
                                        Width="120"
                                        Command="{TemplateBinding RemoveActivationKeyProfileCommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
